Use `kz-air-quality-review.tex` as the main file in Overleaf.

Required files already included:

- `kz-air-quality-review.tex`
- `kz-air-quality-review-bib.bib`
- `sn-jnl.cls`
- `sn-mathphys-num.bst`

Reference template files also included:

- `sn-article.tex`
- `sn-bibliography.bib`
- `user-manual.pdf`

If citations appear as `[?]` in Overleaf:

1. Set `kz-air-quality-review.tex` as Main file.
2. Run `Recompile from scratch`.
3. Ensure the compiler uses the standard LaTeX/BibTeX workflow.
